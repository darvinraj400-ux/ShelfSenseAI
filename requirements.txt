# ShelfSenseAI — Python dependencies
# Direct dependencies only (the packages the app actually imports),
# pinned to the versions verified in the development environment.
# Install with:  pip install -r requirements.txt

# --- Web framework, forms, sessions ---
Flask==3.1.3
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
WTForms==3.2.2
email-validator==2.3.0

# --- Database ---
SQLAlchemy==2.0.51
PyMySQL==1.2.0

# --- Configuration ---
python-dotenv==1.2.2

# --- PriceCatcher data import (import_pricecatcher.py) ---
pandas==3.0.5
pyarrow==25.0.0

# --- Product matching (Phase 3C: services/matching.py) ---
rapidfuzz==3.14.5

# --- ML pricing engine (Phase 3E: services/pricing_engine.py) ---
scikit-learn==1.9.0
joblib==1.5.3

# --- LLM explanations (Phase 3F: services/llm_explainer.py) ---
google-genai==2.19.0
